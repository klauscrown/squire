import { defaultVisualTheme, type GrimoireTheme } from './visual';

/**
 * Tokens visuais do grimório digital — tema Arcano (padrão).
 * Preferir `useGrimoire()` / `useVisualTheme()` para reagir à troca de tema.
 */
export const grimoire = defaultVisualTheme.grimoire;

export type { GrimoireTheme };
